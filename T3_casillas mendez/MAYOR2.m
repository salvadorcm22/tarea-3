promt=('Intoduce un valor: ')
a=input (promt);
promt=('Intoduce un valor: ')
b=input (promt);
promt=('Intoduce un valor: ')
c=input (promt);
if(a>b & a>c);
 sprintf('El numero mayor es:%d;', a)
else
 if(b>a & b>c);
  sprintf('El numero mayor es:%d;', b)
  else
  sprintf('El numero mayor es:%d;',c)
  end
end